{
  "contexts": {
    "homelab": {
      "git_include_file": "homelab_hosts",
      "domain": "lan.example.net",
      "credential": {
        "username": "alice"
      }
    },
    "work": {
      "git_include_file": "work_hosts",
      "domain": "corp.example.com",
      "credential": {
        "username": "alice",
        "password": "work_network_password"
      }
    }
  },
  "hosts": {
    "legacy-switch": {
      "credentials": [
        {
          "username": "alice",
          "password": "legacy_device_password",
          "default": true
        }
      ]
    },
    "router": {
      "credentials": [
        {
          "username": "alice",
          "password": "router_specific_password",
          "default": true
        },
        {
          "username": "netadmin",
          "password": "router_admin_password"
        }
      ]
    }
  }
}
